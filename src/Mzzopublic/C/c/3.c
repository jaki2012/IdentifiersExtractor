/* Input two numbers, output the product */
#include <stdio.h>
main()
{
		int x,y,m;				/*  x y m */
		printf("Please input x and y\n");	/*   */
		scanf("%d%d",&x,&y);			/*  x y  */
		m=x*y;					/*  m */
		printf("%d * %d = %d\n",x,y,m);		/*   */
}